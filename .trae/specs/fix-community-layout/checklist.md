- [x] `src/assets/tabbar/` 下所有的图片文件（8 个）都是有效的图片，而非空文件。
- [x] 社区页面的 Header 不再遮挡下方内容，采用 `flex-col` 和 `flex-1` 完美切分布局。
- [x] 卡片具有精致的内边距，统一的 `rounded-2xl` 或 `rounded-[24px]` 外观。
- [x] 用户头像、昵称和时间排版紧凑不被截断，且字体清晰易读。
- [x] 正文、配图与评论区之间的间距和谐，配图为圆角（Inset）。
- [x] 底部最后一条动态可以通过 `ScrollView` 完整滚动到可视区域，不会被悬浮按钮和 TabBar 永久遮挡。
- [x] 悬浮按钮位置合理，阴影和色彩有质感，且不会产生遮挡卡片关键区域的错觉。